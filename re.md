

### 💻 1. Fundamentos e Estrutura do HTML

1.  **Conceitos Básicos:**
    * O que é HTML (Linguagem de Marcação de Hipertexto) e sua função (estrutura e conteúdo).
    * Estrutura básica de um documento HTML (`<!DOCTYPE>`, `<html>`, `<head>`, `<body>`).
    * A diferença entre *tags* de abertura, fechamento e *self-closing* (vazias).
    * Comentários em HTML.

2.  **Principais Elementos:**
    * **Textos:** Títulos (`<h1>` a `<h6>`), parágrafos (`<p>`), formatação de texto (`<strong>`, `<em>`, `<span>`).
    * **Links:** Âncoras (`<a>`) e seus atributos (como `href` e `target`).
    * **Imagens e Multimídia:** Imagens (`<img>`), vídeos (`<video>`), áudios (`<audio>`) e seus atributos (como `src` e `alt`).
    * **Listas:** Não ordenadas (`<ul>`, `<li>`) e ordenadas (`<ol>`, `<li>`).

3.  **Estrutura Semântica:**
    * Importância da semântica.
    * Elementos semânticos (como `<header>`, `<nav>`, `<main>`, `<article>`, `<section>`, `<footer>`).

4.  **Formulários:**
    * A tag `<form>`.
    * Tipos de entrada (`<input>` com `type="text"`, `type="submit"`, `type="radio"`, `type="checkbox"`, etc.).
    * Botões (`<button>`).
    * Seletores (`<select>`, `<option>`) e áreas de texto (`<textarea>`).

---

### 🎨 2. Fundamentos e Estilização com CSS

1.  **Conceitos Básicos:**
    * O que é CSS (Folhas de Estilo em Cascata) e sua função (estilo e apresentação).
    * A regra de estilo (seletor, propriedade, valor).
    * Formas de aplicar CSS: *inline*, interno (`<style>`) e **externo** (via `<link>`).
    * A **Cascata**, **Especificidade** e **Herança** do CSS.

2.  **Seletores Essenciais:**
    * Seletores de elemento (por tag).
    * Seletores de **classe** (`.nome-da-classe`) e **ID** (`#id-do-elemento`).
    * Seletores combinadores (descendente, filho, etc.).
    * Seletores de atributo e pseudo-classes (como `:hover`, `:nth-child`).

3.  **Propriedades de Estilo:**
    * **Cores e Fundos:** `color`, `background-color`, `background-image`.
    * **Tipografia:** `font-family`, `font-size`, `font-weight`, `text-align`.

4.  **Modelo de Caixa (Box Model):**
    * Compreensão de `content`, **`padding`**, **`border`** e **`margin`**.
    * Propriedade `box-sizing` (`border-box`).

---

### ⚙️ 3. Layout e Design Responsivo

1.  **Posicionamento e Display:**
    * Propriedade `display` (`block`, `inline`, `inline-block`, `none`).
    * Propriedade `position` (`static`, `relative`, `absolute`, `fixed`, `sticky`).
    * Flutuação (`float`) e `clear` (para conceitos de layout mais antigos, mas úteis para entender a história).

2.  **Layout Moderno (Obrigatório):**
    * **Flexbox (Display Flex):** Criação de layouts unidimensionais, alinhamento (`justify-content`, `align-items`) e ordem.
    * **CSS Grid (Display Grid):** Criação de layouts bidimensionais, definição de colunas e linhas.

3.  **Design Responsivo:**
    * Conceito de *Mobile First*.
    * **Media Queries:** Uso de `@media` para aplicar estilos em diferentes tamanhos de tela.
    * Unidades relativas (`%`, `em`, `rem`, `vh`, `vw`) em contraste com unidades fixas (`px`).

---

### 🚀 4. Boas Práticas e Ferramentas

* **Organização de Código:** Nomeclatura de classes (como BEM - Bloco, Elemento, Modificador).
* **Performance e Boas Práticas:** Minificação de código, acessibilidade básica (uso de atributos `alt`, contraste de cores).
* **Ferramentas:** Uso das *Developer Tools* do navegador para inspeção e *debug*.

---
